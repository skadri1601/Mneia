# The inventory of what apps/web reads in production. Names and purposes here, VALUES NEVER.
#
# On the droplet this lives at /etc/mneia/web.env, which deploy/docker-compose.yml loads
# via env_file. That file is not in the repository and must never be — this one exists so
# the set of variables, and what breaks without each, is reviewable in a diff.
#
# After changing anything here, restart the service: the app reads process.env at runtime.

# ---------------------------------------------------------------------------
# Required — the app cannot serve without these
# ---------------------------------------------------------------------------

# Direct Neon connection string. The role must NOT hold SUPERUSER or BYPASSRLS:
# every store in apps/web calls assertConnectionEnforcesRls per transaction and will
# refuse the connection. Provision one with `node scripts/db-provision-app-role.mjs --apply`.
# Check the live posture with: curl -s https://app.mneia.dev/api/health
DATABASE_URL=

# Clerk. The publishable key is also needed at BUILD time — deploy-web.yml passes it as a
# Docker build-arg, so changing it means rebuilding, not just restarting.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# These two are the exception to this file: leave them blank here. MNE-265 moved
# them to the OPENAI_API_KEY and ANTHROPIC_API_KEY repository secrets, which
# deploy-web.yml passes to docker compose, and the environment: block in
# docker-compose.yml wins over this env_file. Rotating a key is therefore a
# gh secret set plus a re-run of the deploy, with nothing to edit on the droplet
# and no key sitting in a file a person has to remember to update.

# OpenAI. Serves two calls: extraction on checkpoint (gpt-5.6-luna) and the
# embeddings that make semantic ranking work at all (text-embedding-3-small).
# Without it, checkpoint cannot propose anything and semanticRelevance — 30% of
# the rehydration score — stays a neutral constant.
OPENAI_API_KEY=

# Anthropic. The extraction fallback only. It is used when OpenAI returns 5xx,
# 429, or times out, which is why the published subprocessor table still names
# Anthropic. Without it a single-vendor outage takes checkpoint down entirely.
ANTHROPIC_API_KEY=

# ---------------------------------------------------------------------------
# Required to admit anyone from the waitlist (MNE-233)
# ---------------------------------------------------------------------------

# Comma-separated Clerk user ids allowed to open /admin. If this is unset, /admin denies
# EVERYONE — including the founder — and the waitlist queue is unreachable even though the
# code is fine. This is the single most common reason admission appears broken.
MNEIA_SUPER_ADMIN_SUBJECTS=

# Resend credentials for the access email. If either is unset, approving a signup is
# REFUSED rather than approving someone we then cannot reach.
RESEND_API_KEY=
WAITLIST_FROM=

# Sender for the team invitation email, and a SEPARATE address from WAITLIST_FROM above —
# deliverInvitationEmail reads this one and nothing falls back to the waitlist sender. Unset
# it and every invitation lands on /team?error=invite_email_failed: the invitation row and the
# join link are still created, so the lead can send the link by hand, but no email is sent and
# nothing else reports why. The domain must be verified in Resend.
MNEIA_INVITE_FROM=

# ---------------------------------------------------------------------------
# Required to find out that anything broke (MNE-86)
# ---------------------------------------------------------------------------

# Sentry DSN for apps/web. docs/DEPLOY.md has listed this since launch, but until MNE-86
# nothing in apps/web read it — only apps/site was instrumented — so every unhandled 500 in
# the product API went to no Sentry project, no log, and no alert.
#
# That is not hypothetical: mneia_rehydrate returned 500 with an empty body for eleven days
# on the only project holding real items, and it was found by hand rather than reported.
# Unset this and serve.ts still returns the bodiless 500; the difference is whether anyone
# learns it happened. It is separate from NEXT_PUBLIC_SENTRY_DSN, which is the browser key.
#
# /api/health now reports this as errorReporting: configured | no_dsn, so whether the
# deployment can report an error is answerable without deploying anything to find out.
SENTRY_DSN=

# Arms POST /api/error-probe, which raises a deliberate unhandled error so you can confirm
# it arrives in Sentry from the deployed app. Unset — the normal state — the route 404s and
# is indistinguishable from a route that does not exist, so it cannot be used to burn the
# Sentry quota. Set it to a long random string only while confirming, then unset it.
# Send it as the x-mneia-error-probe header; ?label=<text> names the event.
MNEIA_ERROR_PROBE_TOKEN=

# ---------------------------------------------------------------------------
# Optional — sensible defaults, set them only to override
# ---------------------------------------------------------------------------

# Origin of the app itself, used to build device-flow verification URLs.
# Defaults to https://app.mneia.dev
# MNEIA_APP_ORIGIN=

# Origin of the marketing site, used for links inside the access email.
# MNEIA_SITE_ORIGIN=

# §17 events are written to the telemetry_event table by default — the moat is not opt-in,
# so there is no variable to set to turn capture on. /api/health reports "telemetry":"persisted"
# when they are landing. The two variables below only ever turn capture DOWN.
#
# MNEIA_TELEMETRY_STORE=off disables the Postgres sink. With no MNEIA_TELEMETRY_PATH set that
# discards every event, health reports "dropped", and the deploy fails — which is deliberate,
# because losing the arbitration dataset is not something that should happen quietly.
# MNEIA_TELEMETRY_STORE=

# MNEIA_TELEMETRY=off is the MNE-50 opt-out and silences the spine entirely. Health reports
# "opted_out" and does not degrade, because opting out is a right rather than a fault.
# MNEIA_TELEMETRY=

# Absolute path for an additional JSONL copy of the §17 events. Optional, and not a
# substitute for the table: a file on this container's disk is not queryable and not backed up.
# MNEIA_TELEMETRY_PATH=

# Remote sink, for a surface that cannot reach Postgres directly. The web app does not use it.
# MNEIA_TELEMETRY_ENDPOINT=
# MNEIA_TELEMETRY_TOKEN=

# The three model variables below are ALLOWLISTED IN CODE, not passed through.
# Between them the whole product can only ever call three models:
#
#   extraction primary   gpt-5.6-luna
#   extraction fallback  claude-haiku-4-5
#   embeddings           text-embedding-3-small
#
# Anything else is refused before a request is sent, so a typo or a copied config
# fails loudly instead of quietly billing a model nobody sized. Adding a model is a
# code change and a PR — deliberately, because that is the review this deserves.
#
# Belt and braces on the OpenAI side: its Project settings support a model
# allow_list, so the key itself can be restricted to gpt-5.6-luna and
# text-embedding-3-small. Anthropic has no per-model restriction — workspaces cap
# spend and rate, not model choice — so for Haiku the code allowlist is the only
# control there is.

# Which model extracts items from a trajectory. Only gpt-5.6-luna is accepted.
# MNE-180 exists to tune this against real sessions; the ledger in
# checkpoint_usage is what it reads.
# MNEIA_EXTRACTION_MODEL=

# The fallback, used only on 5xx, 429, or timeout from the primary. Only
# claude-haiku-4-5 is accepted. Deliberately a different vendor: an OpenAI outage
# would otherwise take checkpoint down entirely, and checkpoint is the one
# operation that cannot degrade. Note its 200K context is smaller than the
# primary's 1.05M — one session in 413 measured did not fit.
# MNEIA_EXTRACTION_FALLBACK_MODEL=

# Which model computes embeddings. Only text-embedding-3-small is accepted, at
# 1536 dimensions, ruled in MNE-176. A different model is refused rather than
# allowed to write vectors nothing can rank against: stored vectors are keyed
# (item_id, model) and are only comparable against their own model. Swapping is a
# backfill, which is MNE-56, not an environment variable.
# MNEIA_EMBEDDING_MODEL=

# ---------------------------------------------------------------------------
# Rate limits and abuse controls (MNE-173)
# ---------------------------------------------------------------------------
# Every one of these has a working default, so leaving them unset is safe. They exist
# so a limit can be raised for an account without a deploy. A non-positive or
# non-numeric value is rejected at startup rather than silently falling back.
#
# Hosted-only means we absorb every request, and checkpoint is the expensive call —
# it is the one that reaches a model. Reads are capped loosely, checkpoints tightly,
# and the workspace-daily ceiling is the backstop against a runaway CI loop.

# Requests per minute, per API token. Default 120.
# MNEIA_RATE_LIMIT_REQUESTS_PER_MINUTE=

# Checkpoints per hour, per API token. Default 60.
# MNEIA_RATE_LIMIT_CHECKPOINTS_PER_HOUR=

# Checkpoints per day, per workspace — keyed on the workspace, not the token, so
# issuing a fresh token does not reset it. Default 200.
#
# This was 500 while checkpointing was something a human triggered by hand. Auto
# triggering changes the exposure: at roughly $0.007 of inference per checkpoint,
# 500 a day is about $105 a month for a single free solo workspace, and §14 says
# the solo tier stays free. 200 is still far above any real usage pattern.
# MNEIA_RATE_LIMIT_CHECKPOINTS_PER_DAY=

# Largest accepted request body in bytes. An unbounded transcript is an unbounded
# prompt. Default 1048576 (1 MiB).
# MNEIA_MAX_REQUEST_BYTES=

# From address on a workspace invitation email (MNE-252). Deliberately separate
# from WAITLIST_FROM: an invitation is transactional mail to an address a customer
# supplied, not a message to the waitlist, and the two must not share an identity
# or a send log. Unset means the invitation is still created and its join link is
# still shown, but no email is sent and the inviter is told the send failed.
# MNEIA_INVITE_FROM=

# Stripe, for the team tier at $24 per user per month (MNE-141/142/143). All three
# must be set or none of the billing path runs: /api/stripe/webhook answers 503
# not_configured and no workspace changes plan. /api/health reports this as
# billing: not_configured, so it is a question with an answer rather than a belief.
#
# Do NOT set these here. Like the model keys, they are repository secrets that
# deploy-web.yml exports into the container - editing the droplet is overwritten
# by the next deploy. Rotating one is `gh secret set` plus a re-run of the deploy.
# STRIPE_SECRET_KEY=
# STRIPE_PRICE_ID=
# STRIPE_WEBHOOK_SECRET=

# ---------------------------------------------------------------------------
# Must NOT be set in production
# ---------------------------------------------------------------------------

# Setting this to 1 disables the row-level-security guard on every connection, which
# makes every workspace-isolation policy inert (§11.3). /api/health reports
# rls=bypassed_by_escape_hatch when it is set, so the state is visible rather than silent.
# MNEIA_ALLOW_RLS_BYPASS=
