---
'@mneia/core': minor
---

Render the handoff artifact. `renderHandoff` produces the §10.3 eight-section markdown from real project state, with one provenance line format used by every item, and a Superseded Recently block that carries why each item went.
